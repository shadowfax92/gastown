description = "Monorepo agent workflow with TDD: extends mol-agent-work-monorepo, expands the implement step with a red-green-refactor cycle."
extends = ["mol-agent-work-monorepo"]
formula = "mol-agent-work-monorepo-tdd"
type = "workflow"
version = 1

[compose]

[[compose.expand]]
target = "implement"
with = "tdd-cycle"
